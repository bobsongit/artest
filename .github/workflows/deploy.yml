name: Install dependencies and deploy

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download and extract library files
        run: |
          mkdir -p vendor
          npm pack aframe@1.3.0
          tar -xzf aframe-1.3.0.tgz
          cp package/dist/aframe-v1.3.0.min.js vendor/aframe.min.js
          rm -rf package aframe-1.3.0.tgz

          npm pack @ar-js-org/ar.js@3.4.5
          tar -xzf ar-js-org-ar.js-3.4.5.tgz
          cp package/aframe/build/aframe-ar-nft.js vendor/arjs-nft.js
          rm -rf package ar-js-org-ar.js-3.4.5.tgz

          echo "vendor/ contents:"
          ls -lh vendor/

      - name: Commit and push vendor/ files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main
          git add vendor/
          git status
          git diff --staged --quiet && echo "No changes to commit" || (git commit -m "Auto: add vendor files" && git push origin main)
